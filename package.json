{
  "name": "blog",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "npx @11ty/eleventy --serve",
    "build": "NODE_ENV=production node scripts/optimize-photos.mjs && npx @11ty/eleventy",
    "preview": "npx wrangler pages dev dist"
  },
  "dependencies": {
    "@11ty/eleventy": "^3.0.0",
    "@11ty/eleventy-plugin-rss": "^2.0.0",
    "@kevingimbel/eleventy-plugin-mermaid": "^1.0.0",
    "js-yaml": "^4.1.0"
  },
  "devDependencies": {
    "sharp": "^0.34.5",
    "wrangler": "^4.86.0"
  }
}
